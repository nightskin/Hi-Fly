using UnityEditor;
using UnityEngine;

public class LockToTarget : MonoBehaviour
{
    [SerializeField] Transform target;

    Vector3 position;

    void Update()
    {
        if(target != null)
        {
            
        }
    }
}
